\documentclass[border=10pt]{standalone}
\usepackage{diagram_styles}

\begin{document}
\begin{tikzpicture}[node distance=0.8cm, >=Stealth]

  % ── Input (gray) ──
  \node[inputblock] (input) {Single\\Image};

  % ── Backbone ──
  \node[backboneblock, right=1.0cm of input, minimum height=1.2cm, minimum width=2.8cm] (vitl)
    {DINOv2 ViT-L/14\\{\scriptsize 1024-dim}};
  \draw[dataflow] (input) -- (vitl);

  % ── Light-DPT ──
  \node[frozenblock, right=1.2cm of vitl, minimum width=2.4cm, minimum height=1.0cm] (dpt)
    {Light-DPT\\{\scriptsize CNN decoder}\\{\scriptsize $+$ ray head}};
  \draw[dataflow] (vitl) -- (dpt);
  \path (vitl.east) -- (dpt.west) node[flowlabel, midway, above]
    {\scriptsize 4 scales};

  % ── Closed-form K (gray) ──
  \node[ndblock, right=1.2cm of dpt, minimum width=2.4cm, minimum height=1.0cm] (K)
    {Closed-form $\mathbf{K}$\\{\scriptsize rays $\to$ $A\mathbf{x}{=}\mathbf{b}$}\\{\scriptsize $[f_x, f_y, c_x, c_y]$}};
  \draw[dataflow] (dpt) -- (K);
  \path (dpt.east) -- (K.west) node[flowlabel, midway, above]
    {\scriptsize FoV field};

\end{tikzpicture}
\end{document}
